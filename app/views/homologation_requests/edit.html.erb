<% content_for :title, t("requests.edit_title") %>

<header class="mb-8">
  <h1 class="t-heading-xl"><%= t("requests.edit_title") %></h1>
  <p class="text-mute mt-2 max-w-2xl"><%= t("requests.edit_lede") %></p>
</header>

<%= render "form", homologation_request: @homologation_request %>
