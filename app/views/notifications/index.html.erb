<% content_for :title, t("nav.notifications") %>

<%= render "shared/page_header", title: t("nav.notifications") %>

<% if @notifications.any? %>
  <div id="notifications" class="space-y-2">
    <%= render @notifications %>
  </div>

  <% if @pagy.pages > 1 %>
    <div class="flex justify-center mt-6">
      <div class="join">
        <% if @pagy.page > 1 %>
          <%= link_to "«", url_for(page: @pagy.page - 1), class: "btn btn-sm join-item" %>
        <% else %>
          <span class="btn btn-sm join-item btn-disabled">«</span>
        <% end %>

        <span class="btn btn-sm join-item pointer-events-none">
          <%= @pagy.page %> / <%= @pagy.pages %>
        </span>

        <% if @pagy.page < @pagy.pages %>
          <%= link_to "»", url_for(page: @pagy.page + 1), class: "btn btn-sm join-item" %>
        <% else %>
          <span class="btn btn-sm join-item btn-disabled">»</span>
        <% end %>
      </div>
    </div>
  <% end %>
<% else %>
  <div class="card bg-base-100 shadow">
    <div class="card-body items-center text-center py-16">
      <p class="opacity-60"><%= t("notifications.empty") %></p>
    </div>
  </div>
<% end %>
